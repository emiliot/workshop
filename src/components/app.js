import React from 'react';
const styles = require('./styles.css')

console.log(styles)
const App = () => (
    <div className={styles.app}>
        <h2>Hello, emilio</h2>
    </div>
);

export default App;